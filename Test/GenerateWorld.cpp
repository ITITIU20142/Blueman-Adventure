#include<bits/stdc++.h>

#define openr(a) freopen(a, "r", stdin)
#define openw(a) freopen(a, "w", stdout)

using namespace std;

signed main() {
    openr("GenerateWorld.txt");
    ios::sync_with_stdio(false); 
    cin.tie(NULL), cout.tie(NULL);

    for(int i = 0; i < 25; i++) {
        for(int j = 0; j < 25; j++) {

        }
    }

    return 0;
}